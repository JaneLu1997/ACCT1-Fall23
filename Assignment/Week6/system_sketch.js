let domino =[]
let force
let tiltAng
let gravity

function setup(){
    createCanvas(windowWidth, windowHeight)
    colorMode(HSB, TWO_PI, 1, 1)

    force = rotate()

}

function draw(){
    background(0)
}